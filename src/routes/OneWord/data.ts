export const pointsList = [
  'ReactDOM.render() is for SPA, ReactDOM.hydrate() is for SSR',
  'Before we use this inside class based component, we must introduce super() inside constructor',
  'Why/Purpose <RestrictMode />: Show warnings, such as potential side effects occur inside current component, or some syntax warnings if not following the suggestions from React',
  'When to use `useRefs`: trigger animation / media player / third party library integration',
  'Props: read-only & cannot be modified; State: changes can be asynchronous & can be modified be setState() or hooks functions',
  'Stateless components only have props, stateful components can have states and props',
  'For quick form build tools: `npm i formik yup`',
];
